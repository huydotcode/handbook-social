{
  "name": "handbook-monorepo",
  "private": true,
  "packageManager": "pnpm@9.12.0",
  "scripts": {
    "dev:client": "pnpm --filter client dev",
    "dev:server": "pnpm --filter server dev",
    "dev:realtime": "pnpm --filter realtime-server dev",
    "dev": "pnpm run /^dev:/",
    "build": "pnpm --filter shared build && pnpm --filter server build && pnpm --filter realtime-server build && pnpm --filter client build",
    "build:client": "pnpm --filter shared build && pnpm --filter client build",
    "build:server": "pnpm --filter shared build && pnpm --filter server build",
    "build:realtime": "pnpm --filter shared build && pnpm --filter realtime-server build",
    "build:shared": "pnpm --filter shared build"
  },
  "devDependencies": {
    "@types/bcrypt": "^5.0.0",
    "@types/jest": "^29.5.13",
    "@types/jsonwebtoken": "^9.0.6",
    "@types/node": "24.3.1",
    "@types/react": "19.0.2",
    "@types/react-dom": "19.0.2",
    "typescript": "^5.9.2"
  }
}
